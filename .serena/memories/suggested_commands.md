# Suggested Commands

```bash
npm run dev      # Start dev server (Next.js, Turbopack default)
npm run build    # Production build
npm run start    # Serve production build
npm run lint     # ESLint (flat config, eslint-config-next core-web-vitals + typescript)
```

No test/typecheck script defined in package.json (`tsc --noEmit` can be run ad hoc if type-checking is needed; not wired to a script).

Darwin/zsh: standard unix utils (`git`, `ls`, `grep`, `find`) behave as usual — no BSD/GNU divergences noted so far.
